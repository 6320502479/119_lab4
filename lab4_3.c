#include <stdio.h>
#include <string.h>
int main()
{
    char in[200],out[50];
    int i,j,n;
    scanf("%[^\n]s",in);
    puts(in);
    n=strlen(in);
    out[0]=in[1];
    puts(out);
    for(i=0;i<n;i++)
    {

    }
}
